app.controller('ContactStateController', ContactStateController);

ContactStateController.$inject = ['$scope', '$animate', '$mdDialog'];
function ContactStateController (  $scope,   $animate,   $mdDialog) {

  class ContactStateController {

    constructor () {
      
    }

  }

  return new ContactStateController();

}